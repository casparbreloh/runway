{
  "name": "runway-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:sandbox": "pnpm --filter runway-sandbox build",
    "dev": "pnpm build:sandbox && wrangler dev",
    "deploy": "pnpm build:sandbox && wrangler deploy",
    "cf-typegen": "wrangler types",
    "format": "oxfmt",
    "format-check": "oxfmt --check",
    "lint": "oxlint",
    "typecheck": "tsgo --noEmit && pnpm --filter runway-sandbox typecheck"
  },
  "dependencies": {
    "@linear/sdk": "catalog:",
    "runway": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.5.0"
    },
    "runtime": {
      "name": "node",
      "version": "^26"
    }
  },
  "engines": {
    "node": "^26"
  },
  "packageManager": "pnpm@11.5.0"
}
