{
  "name": "runway-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "oxfmt",
    "format-check": "oxfmt --check",
    "lint": "oxlint",
    "test": "pnpm -r --filter './packages/*' --if-present test",
    "typecheck": "pnpm -r --filter './packages/*' --if-present typecheck && pnpm --filter example typecheck"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.5.0"
    },
    "runtime": {
      "name": "node",
      "version": "^26"
    }
  },
  "engines": {
    "node": "^26"
  },
  "packageManager": "pnpm@11.5.0"
}
