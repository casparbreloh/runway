{
  "name": "runway-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Node code baked into the Runway sandbox image. Runs in the sandbox, never the Worker.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "catalog:",
    "runway": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
