{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "runway build",
    "deploy": "runway deploy",
    "test": "vitest run src"
  },
  "dependencies": {
    "@runway/cloudflare": "workspace:*",
    "@runway/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
