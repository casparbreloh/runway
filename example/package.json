{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "runway build",
    "deploy": "runway deploy",
    "test": "node --test src/*.test.ts"
  },
  "dependencies": {
    "@runway/cloudflare": "workspace:*",
    "@runway/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
